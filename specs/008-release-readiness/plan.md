# Implementation Plan

Keep release readiness operational rather than aspirational: CI executes the
same two commands a contributor runs, while a Windows packaging workflow
builds the installer and retains it for review without publishing. Guides state
the current manual release boundary. Templates make expected context visible at
the point of submission.
