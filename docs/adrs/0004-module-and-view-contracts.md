# Module and view contracts

Every module has a validated manifest that declares its identity, views, collections, commands, events, capabilities, and migrations. App views point to an entry inside the reviewed module bundle.

This lets AI-Mo-To inspect what a module claims to provide before installing it. A generated app can also propose how its views appear in the workspace layout, but that layout change is part of the same reviewable proposal.
